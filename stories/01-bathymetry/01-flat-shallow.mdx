# Flat Bottom (Shallow)

import { SingleSnapshot } from '@stories/components/SingleSnapshot';
import { PROGRESSION_FLAT_SHALLOW } from '@src/render/bathymetryProgressions';

<SingleSnapshot progression={PROGRESSION_FLAT_SHALLOW} colorScale="depth" />

Constant shallow depth (25%) across the entire grid. Waves interact strongly with the bottom everywhere, causing uniform shoaling effects.
