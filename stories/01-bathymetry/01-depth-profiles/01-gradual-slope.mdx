# Gradual Slope

import { SingleSnapshot } from '@stories/components/SingleSnapshot';
import { PROGRESSION_GRADUAL_SLOPE } from '@src/render/bathymetryProgressions';

<SingleSnapshot progression={PROGRESSION_GRADUAL_SLOPE} colorScale="depth" />

Gentle depth gradient starting at moderate depth (50%). Waves slow down gradually and predictably as they approach shore. Compare with Steep Slope which starts at 100% depth.
