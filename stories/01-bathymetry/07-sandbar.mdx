# Sandbar

import { SingleSnapshot } from '@stories/components/SingleSnapshot';
import { PROGRESSION_SANDBAR } from '@src/render/bathymetryProgressions';

<SingleSnapshot progression={PROGRESSION_SANDBAR} colorScale="depth" />

Shallow sandbar in mid-water creates a secondary breaking zone. Waves may break twice - once over the bar, then again at shore.
