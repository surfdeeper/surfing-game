# Channel

import { SingleSnapshot } from '@stories/components/SingleSnapshot';
import { PROGRESSION_CHANNEL } from '@src/render/bathymetryProgressions';

<SingleSnapshot progression={PROGRESSION_CHANNEL} colorScale="depth" />

Deep channel between shallow areas (river mouth pattern). The channel remains deeper while sides are shallower, creating interesting wave refraction patterns.
