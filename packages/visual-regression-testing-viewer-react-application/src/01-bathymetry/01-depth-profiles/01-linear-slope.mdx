# Linear Slope

import { SingleSnapshot } from '@stories/components/SingleSnapshot';
import { PROGRESSION_LINEAR_SLOPE } from '@src/render/bathymetryProgressions';

<SingleSnapshot progression={PROGRESSION_LINEAR_SLOPE} colorScale="depth" />

Constant gradient from deep water (top) to shore (bottom). Simple, predictable wave behavior - waves slow down uniformly as they approach shore.
