# Breaking Criterion

import { Filmstrip, renderMatrixToCanvas } from '@stories/components/Filmstrip';
import { energyToColor } from '@src/render/colorScales';
import { WAVE_BREAKING_STRIP_CRITERION } from './01-breaking-criterion';

export const renderEnergy = (snap, ctx, w, h) =>
  renderMatrixToCanvas(ctx, snap.matrix, energyToColor, w, h);

<Filmstrip snapshots={WAVE_BREAKING_STRIP_CRITERION.snapshots} renderSnapshot={renderEnergy} testId={WAVE_BREAKING_STRIP_CRITERION.testId} />

A wave breaks when `H/d > 0.78` (McCowan criterion).
